{
  "name": "project-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Project Tracker (Express + MongoDB + static client)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
